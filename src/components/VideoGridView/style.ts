import styled from 'styled-components';

export const VideoGridViewContainer = styled.div`
    width: 100%;
    flex-grow: 1;

    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    align-items: center;

    border: 1px solid blue;
`;
